# basic variable dec : 
var varName varType = value

# Short var dec: [known as walrus]
varName := value

# comments: [//] [/**/]

# compilation process:
code(pkg,fun,print) -> compiler(go build) -> executable(./hello)

# prefer "Default" types  
When you're super concerned about performance and memory usage.

# Go is statically typed, compiled lang. Go and Rust use very little memory when compared to Java.

# Constants [const keyword], cant use ":=".

* fmt.Printf() - Prints a formatted string to standard out.
* fmt.Sprintf() - Returns the formatted string
  
