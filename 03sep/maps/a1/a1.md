## Assignment 1 (A1): Map Creation & Size Validation

## Question:
Complete the getUserMap function. It takes a slice of names (names []string) and a slice of phone numbers (phoneNumbers []int).

If names and phoneNumbers don't have equal lengths, return nil and an error created with errors.New("invalid sizes").

Otherwise, initialize a map of string keys to user structs, populate it so each name maps to its corresponding user{name, phoneNumber}, and return the map along with a nil error.