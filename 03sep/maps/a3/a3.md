## Assignment Question:

Complete the isValidKey function. Given a type name represented as a string ("string", "int", "slice", "map", "func"):

Check if the type can be legally used as a map key in Go.

Return true if it is valid (e.g., "string", "int").

Return false if it is invalid (e.g., "slice", "map", "func").