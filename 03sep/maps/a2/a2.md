## Assignment Question:

Complete the deleteUser function. It receives a map of users (userMap) and a target name string.

Check if the user exists in the map using the comma-ok idiom (user, ok := userMap[name]).

If the key does not exist, return false.

If the user exists, remove them from the map using delete() and return true.