## Assignment Question
Fix the compile-time bug in the getFormattedMessages function. The function body is correct, but the function signature is missing its return type.

It needs to take a slice of messages (messages []string) and a formatter function (func(string) string), and return a slice of formatted strings ([]string).