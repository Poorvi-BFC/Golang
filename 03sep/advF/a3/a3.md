## Concept Overview
The defer keyword delays the execution of a function until the surrounding function returns.

Deferred calls are executed in LIFO (Last-In, First-Out) order—meaning the last function deferred runs first when the parent function exits.

## Assignment Question
Complete the logActivity function. It takes a activity name string (activity).

Immediately print "Starting activity: <activity>".

Defer printing "Finished activity: <activity>".

Print "Performing step 1".

Print "Performing step 2".

When executed, the deferred print statement should run last, automatically logging the finish message upon function exit.