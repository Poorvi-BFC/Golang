## Concept Overview
In Go, functions are first-class citizens, meaning they can be stored in variables, passed as arguments to other functions, and returned from functions.

A higher-order function is simply a function that receives another function as an argument, returns a function, or both.

## Assignment Question
Complete the aggregate function. It takes three integers (a, b, c) and a formatting function formatter func(int) string.

Compute the sum of a, b, and c.

Pass that sum into the formatter function to get a formatted string.

Return the resulting string.